name: cicd development workflow for dev env

on: 
  push:
    branches:
    - feat/family

jobs:
  hello:
      runs-on: ubuntu-latest
      steps:
        - name: step1
          run: date
        - name: step2
          run: |
            pwd
            ls